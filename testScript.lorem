numerus puts = apere(litera str)

litera[V] yesStr = ['Y', 'e', 's', '\0']
litera[III] noStr = ['N', 'o', '\0']

numerus main = λ():
    asertio bool = II < I
    si(bool):
        puts(yesStr)
    ;
    retro O 
;