numerus puts = apere(litera str)

numerus func = λ(numerus arg):
    arg = arg + I
    retro arg + I
;

numerus main = λ():
    numerus value = V
    numerus newValue = func(value)

    litera letter = XLVIII + newValue 
    litera terminator = '\0'
    puts(letter)

    retro O 
;