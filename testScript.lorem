apere './std.lorem'
apere './binarytree.lorem'

numerus[C] tree
asertio success

createBinaryTree(tree, C, O)

addValue(tree, X)
addValue(tree, V)
addValue(tree, VII)
addValue(tree, XX)

scriborTree(tree)
